import React, { useState, useEffect } from 'react';
import { Modal, Button, Input, message } from 'antd';
import axios from 'axios';

const RecordModal = ({ visible, template, onClose, onSave }) => {
};

export default RecordModal;